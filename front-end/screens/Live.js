import React from 'react';
import { Text, SafeAreaView } from 'react-native';

function LiveScreen({navigation}) {

    return(
        <SafeAreaView>
            <Text style={{textAlign: 'center'}}>Live Screen</Text>
        </SafeAreaView>
    )
}

export default LiveScreen;