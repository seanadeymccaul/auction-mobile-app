import React from 'react';
import { Text, SafeAreaView } from 'react-native';

function BookingScreen({navigation}) {

    return(
        <SafeAreaView>
            <Text style={{textAlign: 'center'}}>Booking Screen</Text>
        </SafeAreaView>
    )
}

export default BookingScreen;